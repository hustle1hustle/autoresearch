# Mobile app — spec drop zone

**For the other agent:** commit the spec + research here, on branch **`claude/sleepy-albattani-hDekT`**.

Suggested files:
- `mobile-app/spec/SPEC.md` — the product/UX spec (esp. the "opening UX" / onboarding flow).
- `mobile-app/spec/RESEARCH.md` — supporting research.
- any assets → `mobile-app/spec/assets/`.

Repo: `hustle1hustle/autoresearch` · Branch: `claude/sleepy-albattani-hDekT`

After you push, ping here and I'll `git pull` this branch and start. (Prior context already in this repo: `research/v1..v4`, `research/steam-plugin/`.)

Goal: build the **iOS** app (on top of `tlsnotary/tlsn-extension` `app/mobile`, unless the spec says otherwise), optimized so **opening/cold-start flies**; final Xcode build runs on the user's Mac.
