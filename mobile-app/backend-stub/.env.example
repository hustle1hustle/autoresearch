# Copy to .env (gitignored) and fill in from your GitHub App (setup/02-oauth-apps.md §A).
PORT=8787
GITHUB_CLIENT_ID=Iv1.replace_me
GITHUB_CLIENT_SECRET=replace_me_server_side_only
# Optional — if set, must exactly match the authorize redirect_uri:
GITHUB_REDIRECT_URI=https://r3p.xyz/oauth/github/callback
